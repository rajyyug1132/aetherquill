[package]
name = "device"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "aetherquill"
path = "src/main.rs"

[dependencies]
recognizer = { path = "../recognizer" }
libc = "0.2"
font8x8 = "0.3"

[profile.release]
lto = true
strip = true
