[package]
name = "recognizer"
version = "0.1.0"
edition = "2021"

# ponytail: no runtime deps yet — pure geometry/parsing needs nothing but std.
[dependencies]

[dev-dependencies]
serde_json = "1" # parity tests read fixtures/pipeline.json
